const config = {
    //Indicate what video should be play.
    entry: "./3.js",
    //Highest frame rate (if possible)
    maxFPS: 20,
    //Tickingarea for all pixel(Max: 300 × 300)
    tickingarea: true
}
export { config };